---
title: 补完 Reaper 效率链的最后一环（三）Command Line 详解
date: 2020-09-28 16:46
---
大家好，我是溪夜。
在《补完 Reaper 效率链的最后一环》专栏的第三篇中暂时先把 ReaScript 放到一边，我们看点别的。
上篇文章的开头曾谈到包括 Reaper、Wwise 和 LMB 都对 Command Line（命令行）有着支持。然而对于这部分功能的使用方法，少见的在 Reaper 帮助文档内居然一句未提，Cockos 论坛上也只有少数帖子有过相关讨论。
**作为对文档欠缺部分的补充，我认为有必要详细聊一聊 Reaper 的 Command Line 用法及其扩展思路。**

> 本系列文章阅读需要的前置知识：
> - 有 Python 基础。
> - 对 macOS 终端、Windows 命令行等略有了解。
> - 了解少量的 Reaper 批处理概念。

本文目录：

[toc]

## 概念与配置方法
### 为什么要用 Command Line（命令行）？
为什么有好好的图形界面不用，非得要通过命令行来使用 Reaper？
**原因很简单：图形界面虽然对用户更友好，但命令行调用可提供更稳定、准确、快速的操作执行结果。**
这也是 Reaper 的伟大之处，一个完整的 DAW 愿意放下身段提供这样的底层使用方式。通过把一部分功能提取出来，可做到同时实现命令行的精准操作与 DAW 级别的后期处理，对于复杂的批处理需求无疑是个杀手锏。
毕竟 FFMpeg 的功能是有限的，想通过它调用成品效果器是无法实现的，Reaper 的命令行就给了这种需求一个很好的补充。
### 查看使用参数文档
对于 Windows 和 macOS 来说，查看命令行文档的方法类似。分别在命令行和终端中输入一下命令（可自行修改为自己的 Reaper 安装目录）：
`Windows: X:\YYY\ZZZ\reaper.exe -h`
`Mac: /Applications/REAPER64.app/Contents/MacOS/REAPER -h`
执行之后可分别看到以下提示（均基于 V6.14）：
![Reaper Windows](/images/Reaper%20Windows-1.png)
Windows
![Reaper macOS](/images/Reaper%20macOS-1.png)
macOS
不难看出，命令行的用法为：`reaper（Reaper 安装路径） [options] [filename.rpp] [filename.wav]`，在 Windows 下横线“-”也可写为“/”。
**对比两者文档的区别，**会发现 macOS 比 Windows 缺少 `-newinst`、`-nonewinst`、`-close` 这三个选项，且 Windows 具有一个独享的选项 `-noactive`，在后面我们会聊一聊它们的用法。
### 添加 Reaper 到环境变量
**你可能会发现，每次都要先输一长串 Reaper 的安装路径再加指令，无疑是比较麻烦的。**
为此，可以把 Reaper 的安装目录添加到环境变量，之后即可直接在命令行中无需目录直接调用 Reaper。关于环境变量的配置方法在此不多加赘述，如有疑问可自行搜索。
下面分别是 Windows 和 macOS 下配置截图及执行结果图：
![Windows 环境变量](/images/Windows%20%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F.png)
在 Windows 环境变量设置中把 Reaper 目录添加到环境变量中
![Windows 环境变量测试](/images/Windows%20%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F%E6%B5%8B%E8%AF%95.png)
在命令行中直接执行 `reaper -h` 可看到与之前相同的结果
![macOS 环境变量](/images/macOS%20%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F.png)
在 macOS 中把 Reaper 目录添加到环境变量中
![macOS 环境变量测试](/images/macOS%20%E7%8E%AF%E5%A2%83%E5%8F%98%E9%87%8F%E6%B5%8B%E8%AF%95-1.png)
在终端中直接执行 `reaper -h` 后也可看到相同的结果
### 功能详解
在开始实例之前，我们先看看 Reaper 命令行工具所提供的功能。**鉴于 Windows 版的功能更多，这部分的介绍以 Windows 版文档为准。**
我们把功能分为三大块。
**第一部分为单一的选项：**
`-audiocfg`：在启动时打开 Preferences 中的 Audio Device 设置窗口
`-cfgfile file.ini`：如果使用其他的资源文件夹需要完整路径，否则会使用默认路径
`-new`：以新的工程启动
`-template filename.rpp`：使用指定的模板工程开启 Reaper
`-saveas newfilename.rpp`：当创建或加载完工程后，另存为指定名称的工程
`-renderproject filename.rpp`：使用工程内保存的渲染设置来渲染指定工程
`-ignoreerrors`：在加载时不会出现报错窗口
`-nosplash`：静默加载，不显示开始画面窗口
`-splashlog /path/to/filename.log`：把开始画面中的家在信息写入到指定路径下的 Log 文件里
`-newinst | -nonewinst`：是否创建新的 Reaper 实例，会忽略设置中对于新实例的检测设置
`-close[all][:save|:nosave]`：关闭工程，可通过选项设置保存与否（无效）
**第二部分为批量转换文本文件中的语法格式：**
`batchconvert filelist.txt`：
批量转换模式，需要使用一个 txt 文件来定义所有的配置。为了方便书写文本格式，我使用代码块窗口来装载配置文件的书写语法（使用 Python 格式渲染）：
```python
# 每行一个原始文件名
filename.wav
# 也可以每行一个原始文件名+制表符+输出文件名
filename.wav(制表符分割)outputfile.wav

# 配置代码模块（可选）
<CONFIG
  SRATE 44100 # 采样率
  NCH 2 # 全局输出的通道数
  RSMODE 7 # 重采样模式，0到9之间，需要在
  DITHER 3 # 抖动处理模式，1为添加抖动，2为噪音塑形，3为两者协作
  USESRCSTART 1 # 把 BWF 内的开始偏置信息应用到输出文件中
  PAD_START 1.0 # 在开头增加静音段落，以秒为单位
  PAD_END 1.0 # 在结尾增加静音段落，以秒为单位
  OUTPATH 'C:\output' # 设置批处理文件的输出目录
  OUTPATTERN '$filenumber_$track' # 使用通配符定义输出文件名，如不设置则使用原文件名
  FXCHAIN 'chain1.RfxChain' # 使用效果器链
  <FXCHAIN # 效果器链子模块，可把效果器链文件内的内容复制在这里，可避免每次都寻找效果器链文件的麻烦
    WAK 0 0
    BYPASS 0 0
    <VST "VST3: PAZ- Analyzer Stereo (Waves)" "WaveShell1-VST3 11.0.vst3" 0 "" 1462399732{5653545741455370617A2D20616E616C} ""
    XXXXXXX # 此处省略效果器链文件内容，太长了
  >
  <OUTFMT # 输出格式子模块，为 base64 格式的数据，可在 RENDER_CFG 中找到对应的数据
    bDNwbVAAAAAAAAAABQAAAP////8EAAAAUAAAAAAAAAA=
  >
  <METADATA # 元数据子模块，可在工程文件的 RENDER_METADATA 中找到对应的数据
    (contents of <RENDER_METADATA block from project file)
  >
>
```
**第三部分为 Windows 独有的选项：**
`-noactivate`：打开 Reaper 后并不前置窗口（无效）
## 简单实例
下面通过一些使用的实例，实际学习一下命令行工具的使用技巧。
### 按需创建新的工程
通过预先创建好的模板文件创建工程：
`reaper -template C:\Users\xiye\AppData\Roaming\REAPER\ProjectTemplates\templates1.rpp`
![结果1](/images/%E7%BB%93%E6%9E%9C1-2.png)

通过预先创建好的模板文件创建工程并另存为 newfilename.rpp（默认保存位置为用户名文件夹下）：
`reaper -template C:\Users\xiye\AppData\Roaming\REAPER\ProjectTemplates\templates1.rpp -saveas newfilename.rpp`
![结果2](/images/%E7%BB%93%E6%9E%9C2-1.png)

渲染刚创建的工程（轨道里已放入音频文件），默认生成的文件会与 Project - Render 设置里的路径相同，如果没指定文件名将会生成一个无名的 wave 文件：
`reaper -renderproject C:\Users\xiye\newfilename.rpp`
![结果3](/images/%E7%BB%93%E6%9E%9C3-2.png)

### 批处理文件
首先创建一个配置文件 convert_setting.txt 供批处理使用：
```python
C:\Users\xiye\Desktop\TestAudio\1.wavC:\Users\xiye\Desktop\TestAudio\2.wavC:\Users\xiye\Desktop\TestAudio\3.wav<CONFIG  OUTPATH "C:\Users\xiye\Desktop\TestAudio\Output"  OUTPATTERN ""  FXCHAIN "chain1.RfxChain"  SRATE 44100  NCH 2  RSMODE 9  DITHER 3  PAD_START 0.0  PAD_END 0.0>
```
在命名行中执行以下命令:
`reaper batchconvert C:\Users\xiye\Desktop\TestAudio\convert_setting.txt`
执行结束后打开 Output 文件夹查看批处理后的音频文件：

## 更深的思考
### 通过 Python 修改 Batch Processing 的文本文件
如果需要通过 Python 修改批处理文件中的字段，可通过简单的代码来实现。
比如我们预先构建好一个 txt 文件作为模板，内容如下：
```python

```
根据其中的内容，可通过如下代码对其的内容进行修改：
```python

```
### 在命令行启动时调用 Action 或 ReaScript
如果想要在命令行调用 Reaper 时引用 Action 或者脚本怎么办呢，我们可以通过一个现有的脚本来做到这件事。
在 Action List 中搜索 
## 接下来讲什么？